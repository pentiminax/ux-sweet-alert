---
title: Utilisation
description: Initialiser le bundle et déclencher des toasts.
---

## Initialisation

Pour afficher automatiquement les toasts et les alertes dans vos templates,
ajoutez la fonction Twig suivante dans votre `base.html.twig` (ou le fichier de layout) :

```twig
{{ ux_sweet_alert_scripts() }}
```

## Toasts

Pour utiliser UX SweetAlert, injectez le service `ToastManagerInterface` et
créez des toasts en PHP :

```php
use Pentiminax\UX\SweetAlert\ToastManagerInterface;

class HomeController extends AbstractController
{
    #[Route('/', name: 'app_homepage')]
    public function index(ToastManagerInterface $toastManager): Response
    {
       $toastManager->success(
            title: 'title',
            text: 'text',
            position: Position::TOP_END,
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true
        );

        return $this->render('home/index.html.twig');
    }
}
```
